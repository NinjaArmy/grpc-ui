# grpc Project for a UserInterface

To run this project you need first to run:

```
npm i
```

Start the server with:

```
node server
```

Start the client with:

```
node client
```

Run a test with:

```
node test
```
